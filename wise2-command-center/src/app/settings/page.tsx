import { AppPlaceholder } from "@/components/app-placeholder";

export default function SettingsPage() {
  return (
    <AppPlaceholder
      title="Settings"
      description="Demo configuration and internal controls are now managed from the Javon admin experience."
      linkLabel="Open demo admin"
      linkHref="/demo/admin"
    />
  );
}
