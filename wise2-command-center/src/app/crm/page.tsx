import { AppPlaceholder } from "@/components/app-placeholder";

export default function CRMPage() {
  return (
    <AppPlaceholder
      title="CRM"
      description="The live CRM walkthrough, pipeline, and lead actions are demonstrated in the Javon experience."
      linkLabel="Open demo CRM"
    />
  );
}
