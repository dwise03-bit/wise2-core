# Security

The bridge requires a bearer token, validates request shapes, limits marker names and track IDs, and defaults to loopback. Use Tailscale ACLs/firewall rules for remote access. Do not commit tokens or bind the bridge publicly.
