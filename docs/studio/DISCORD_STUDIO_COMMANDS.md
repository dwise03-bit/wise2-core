# Discord studio commands

`/studio status`, `project`, `play`, `stop`, `pause`, `record`, `marker name`, `tracks`, `mute track`, `unmute track`, `solo track`, `arm track`, and `render format kind` are registered by `ReaperBot`. Recording requires `Studio Admin`, `Engineer`, or `Producer` (configurable with `WISE2_STUDIO_ROLES`).
