# Deployment status

No workstation or Discord credentials are configured by this change. Deployment requires installing the bridge on the REAPER Mac, configuring the real ReaScript adapter, registering the Discord application command, and validating the end-to-end flow against a test `.rpp` project.
