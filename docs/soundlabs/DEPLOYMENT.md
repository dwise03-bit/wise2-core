# Deployment

Use the repository's existing production compose workflow. Build the website image, refresh the stable website service, then verify `/sound-labs`, APK download URLs, bridge health, and logs. Do not copy environment files or secrets into artifacts.
