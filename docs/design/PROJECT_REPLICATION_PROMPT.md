# WISE² Project Replication Prompt

Apply WISE² Reference Replication Mode for this task.

Read `docs/design/WISE2_UI_CONSTITUTION.md` and the current project's visual-reference lock before touching UI code.

Recreate the supplied approved visual faithfully. Do not redesign, simplify, modernize, or substitute generic SaaS patterns. Preserve all identity-locked assets and existing functionality.

First inspect the repository, current screen, reusable components, brand assets, fonts, and tokens. Identify the major visual mismatches before editing.

Then execute the closed loop:

BUILD -> RUN -> SCREENSHOT -> COMPARE -> LIST DIFFERENCES -> FIX -> SCREENSHOT AGAIN

Prioritize structural and composition differences before micro-polish. Validate desktop, tablet, and mobile. Do not report completion until build/tests and visual QA have been checked, and list any known remaining differences explicitly.
