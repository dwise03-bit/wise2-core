# 2026-09-19 - Daily Log

## Sessions
- Upgraded the WISE² Terminal Dashboard shared WebSocket lifecycle, reconnect behavior, shell working directory, service timeouts, health endpoint, telemetry freshness, and dashboard status styling.

## Verification
- `npm run build` passed in `apps/wise2-terminal-dashboard`.
- Rebuilt `node-pty` for the local Node ABI.
- Terminal dashboard running on `http://localhost:3030`.
- `/health` returned `ok: true`.

## Reflection

## Session 2

- Set WISE² Terminal's default coding path to local Ollama/OpenCode using `ollama/wise2-coder-m4-local`.
- Made the dashboard terminal start in the WISE² Core workspace and documented `wise2 code`.

## Verification

- `bash -n scripts/wise2-terminal.sh` passed.
- Terminal dashboard Vite build passed within the workspace build.
- Full monorepo build remains blocked by the pre-existing `lexis-inks-demo` missing `@wise2/auth` dependency.

**What worked**:
- Auditing the real terminal app first exposed the native dependency mismatch and avoided treating the older runbook as authoritative.

**What didn't work**:
- The checked-in `node-pty` binary was stale for the current Node runtime and required a local rebuild.

**Changes for next time**:
- Add a documented native-dependency bootstrap check to the terminal dashboard setup flow.

## Session 3

- Investigated recurring Ray-Ban 502 errors after deployment.
- Found production drift between competing compose/runtime paths publishing the website on ports 3000 and 3001 while Nginx upstreams also differed.
- Found Ray-Ban API proxy failures caused by duplicate `/api` path composition and build-time use of a localhost fallback instead of the runtime internal API URL.
- Added durable decision `data/decisions/2026-09-19-production-deployment-verification-invariant.md` requiring runtime port inspection and repeated public page/API checks for all WISE² client projects.

**Verification status**:
- The test correctly caught another 502 during container replacement; production is not marked stable until the competing deployment path is removed or standardized.
