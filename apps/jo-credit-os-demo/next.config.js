/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  basePath: '/jo-credit-os',
  assetPrefix: '/jo-credit-os/',
};

module.exports = nextConfig;
