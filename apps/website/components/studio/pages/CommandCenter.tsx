'use client';

export default function CommandCenter() {
  return (
    <div style={{ color: '#666', textAlign: 'center', padding: '40px' }}>
      📊 Command Center — KPI Dashboard, Production Charts, AI Activity Feed
    </div>
  );
}
