import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'WISE SHINE | WISE²', description: 'Premium automotive detailing powered by WISE² systems and standards.' };
export default function WiseShineLayout({ children }: { children: React.ReactNode }) { return children; }
