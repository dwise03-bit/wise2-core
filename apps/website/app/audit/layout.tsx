import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'AI Business Audit | WISE²', description: 'Find the systems and AI opportunities that can move your business forward.' };
export default function AuditLayout({ children }: { children: React.ReactNode }) { return children; }
