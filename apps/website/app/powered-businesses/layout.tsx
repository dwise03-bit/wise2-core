import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Powered Businesses | WISE²', description: 'Explore the businesses and brands powered by the WISE² operating system.' };
export default function PoweredBusinessesLayout({ children }: { children: React.ReactNode }) { return children; }
