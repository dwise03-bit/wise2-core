import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Creative Studio | WISE²', description: 'Create, organize, and ship brand and media work inside the WISE² Creative Studio.' };
export default function StudioLayout({ children }: { children: React.ReactNode }) { return children; }
