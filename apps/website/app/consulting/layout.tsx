import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Consulting | WISE²', description: 'Operator-led strategy, implementation, and growth advisory from WISE².' };
export default function ConsultingLayout({ children }: { children: React.ReactNode }) { return children; }
