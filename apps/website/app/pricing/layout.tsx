import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Pricing | WISE²', description: 'Choose the WISE² plan that fits the way your business operates.' };
export default function PricingLayout({ children }: { children: React.ReactNode }) { return children; }
