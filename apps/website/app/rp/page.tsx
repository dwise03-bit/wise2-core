import { RPHomepage } from '@/components/RPHomepage';

export const metadata = {
  title: 'WISE² RP — Your City. Your Story. Your Legacy.',
  description: 'Build your identity through choices, actions, and relationships. A living roleplay world where your decisions shape your legacy.',
};

export default function RPPage() {
  return <RPHomepage />;
}
