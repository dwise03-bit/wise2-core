import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Sound Labs | WISE²', description: 'Develop music, voice, and sound projects with WISE² Sound Labs.' };
export default function SoundLabsLayout({ children }: { children: React.ReactNode }) { return children; }
