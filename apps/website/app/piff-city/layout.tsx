import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'PIFF CITY | WISE²', description: 'Atlanta culture, sound, media, and experiences powered by WISE².' };
export default function PiffCityLayout({ children }: { children: React.ReactNode }) { return children; }
