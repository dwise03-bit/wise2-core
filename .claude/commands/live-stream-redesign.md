# WISE2 Live Stream Redesign

Load `CLAUDE.md`, then load:

- `promptos/agents/executive.md`
- `promptos/agents/developer.md`
- `promptos/agents/marketing.md`
- `promptos/agents/qa.md`

Coordinate the live-stream redesign workflow:

1. Read existing brand references:
   - `docs/WISE2_DESIGN_SYSTEM_MASTER_VISUAL.png`
   - `docs/BRAND_BIBLE_UPDATED.md`
   - `docs/DESIGN_SYSTEM.md`
   - `.agents/brand-context.md` if present
2. Locate the current live-stream page implementation.
3. Build the real page experience, not a marketing placeholder.
4. Keep visual decisions consistent with the WISE2 brand system.
5. Run targeted lint/build/test checks.
6. Write concise launch copy only after implementation context is known.
7. Append implementation, verification, and remaining gaps to today's daily log.

If required visual assets are missing, use existing brand assets first and ask for user-provided references only when the work cannot proceed responsibly.
