---
name: wise2-mobile-ios
description: Build, test, and diagnose the WISE² iOS app on the Mac.
---
Read `WISE2_UI_CONSTITUTION.md` and `WISE2_WORKFLOW_STANDARD.md`. Use `scripts/wise2-mobile.sh ios`, then `xcodebuild`/`xcrun simctl`; keep signing and provisioning Mac-local.
