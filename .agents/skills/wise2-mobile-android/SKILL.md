---
name: wise2-mobile-android
description: Build, lint, and test WISE² Android projects with Gradle and ADB.
---
Require an actual `apps/wise2-android/gradlew` before running Android commands. Use the Gradle wrapper, `lint`, `test`, `connectedAndroidTest`, and `adb`; never invent missing project structure.
