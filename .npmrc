; Use exact versions from lock file
frozen-lockfile=false

; Prevent network access issues
fetch-timeout=60000
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000

; Allow building without network access to private packages
strict-peer-dependencies=false

; For local workspaces, ensure packages resolve correctly
workspace-root=true
